Enable the repository and install:

```
sudo dnf copr enable kentobias/retch
sudo dnf install retch
```

Builds are provided for Fedora 43 and 44 on `x86_64` and `aarch64`.

See the [documentation wiki](https://github.com/l1a/retch/wiki) for configuration, themes and field reference.
