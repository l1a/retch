[package]
name = "retch-battery"
version = "0.1.0"
edition = "2021"
authors = ["Ken Tobias"]
description = "A lightweight, cross-platform battery information fetching library used by retch"
license = "GPL-3.0"
repository = "https://github.com/l1a/retch"

[dependencies]
# Zero dependencies!
